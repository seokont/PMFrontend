export const getAuth = (state) => state.auth;

export const isLoading = (state) => state.auth.loading;

export const isSuccess = (state) => state.auth.success;
export const isSignUp = (state) => state.auth.isSignUp;
